void main() {
  // # Conditional Expression
  // var is_login = false;
  // var user = is_login ? 'Shiva' : 'Guest';
  // print(user);
  // var name = 'Shiva';
  // here we can put if string is null then display Shiva otherwise
  // String? name = 'Shiva';

  // name = name == null ? null : 'Shiva';
  // var user_new = name ?? 'Guest';
  // print(user_new);
  
}
